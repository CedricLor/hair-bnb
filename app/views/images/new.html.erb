<h1>Add images to your profile</h1>

<%= form_for([@user, @image], html: { multipart: true }) do |f| %>
  <p>
    <%= f.label :picture %>
    <%= f.file_field :picture %>
  </p>
  <p><%= f.submit %></p>
<% end %>
